// Given the String below,perform inbuilt functions in JavaScript to 
// solve it appropriately.
// let x="There is a dog outside"
const message='There is a dog outside'
console.log(message.length)
let stmt='There is a dog outside'
console.log(stmt.includes('there'))
console.log(stmt.toUpperCase())
console.log(message.includes('is a'))
//creating a message on two lines.
let tell="There is my son so grown up,\n intelligent and God fearing."
console.log(tell)
//starts with a particular letter
console.log(message.startsWith('There'))
console.log(message.endsWith('d'))
console.log(indexOf('is'))
